def range_integers_even(maxval):
    """
    Return range of integers from 1 to maxval, inclusive.
    """
    integers = []
    for i in range(maxval):
        i+=1
        if not (i) % 2:
            integers.append(i)
    return integersmathfun?
